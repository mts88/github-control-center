node_modules/
dist/
*.vsix

# Yarn 4 — yarnPath binary in .yarn/releases MUST stay committed
.yarn/*
!.yarn/releases
!.yarn/patches
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# local plans/red-team reviews — never tracked
docs/

# Claude Code worktrees — never tracked
.claude/worktrees/
